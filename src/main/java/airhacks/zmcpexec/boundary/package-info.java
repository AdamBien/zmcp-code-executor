/**
 * Boundary layer for zmcp code executor tool.
 * 
 * Handles external communication with the zmcp protocol and manages
 * the tool's integration with the MCP framework. Contains coarse-grained
 * interfaces for tool discovery and execution.
 */
package airhacks.zmcpexec.boundary;