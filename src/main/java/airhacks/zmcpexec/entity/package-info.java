/**
 * Entity layer for zmcp code executor tool.
 * 
 * Domain objects representing code execution results, error states,
 * and tool metadata. Contains immutable data structures that capture
 * the execution context and outcomes.
 */
package airhacks.zmcpexec.entity;